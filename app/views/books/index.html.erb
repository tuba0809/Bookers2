<% if flash[:notice] %>
    <div class="flash">
         <%= flash[:notice] %>
<% end %>
<body>
  <main>
        <div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
    <%= attachment_image_tag @books.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" %><table class="table">
  <tbody>
  <tr>
	  <th>name</th>
	  <th><%= @users.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= @book.user.introduction %></th>
  </tr>
</tbody></table>

<div class="row">
  <%= link_to '', edit_user_path(@users), class:"btn btn-outline-secondary btn-block fas fa-user-cog" %>

</div>
      <%= form_with model: @books, local:true do |f| %>
      <h2 class="mt-3">New book</h2>
      <form action="/books" accept-charset="UTF-8" method="post"><input name="utf8" type="hidden" value="✓"><input type="hidden" name="authenticity_token" value="cq6XUKH6T1UjWTh7D4d5gCtKSp93UV2+BhAP4s+atwA72y6cgppeq55kT+U1R1izO/cZEJi5TU5yG7mzi5PYBg==">
  <div class="form-group">
    <label for="book_title">Title</label>
      <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <label for="book_opinion">Opinion</label>
    <%= f.text_area :body %>
  </div>
  <div class="form-group">
    <%= f.submit '' %>
    <% end %>
  </div>
</form>


<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
  <% end %>

  <p>投稿ユーザー画像:<%= attachment_image_tag book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p>
  <p>ショップ名：<%= book.title %></p>
  <p>説明：<%= book.body %></p>
  <p>ユーザーネーム：<%= book.user.name %></p>
</div>
<% end %>